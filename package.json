{
  "name": "wayzis",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "db:serve": "json-server --watch saveData.json --port=3001",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "axios": "^0.26.0",
    "json-server": "^0.17.0",
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.2.0",
    "vite": "^2.8.0"
  }
}
